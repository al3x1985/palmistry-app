enum Hand { left, right }

enum PalmShape { square, rectangle, spatulate, conic }

enum LineType { heart, head, life, fate }

enum ScanStatus { processing, editing, completed }

enum LineDepth { deep, medium, faint }

enum LineCurvature { straight, curved, steep }

enum MessageRole { user, assistant }
